#!/usr/bin/env bash
# Stop any running mlx_lm.server processes
set -euo pipefail

pids=$(pgrep -f "mlx_lm.server" 2>/dev/null || true)

if [[ -z "$pids" ]]; then
    echo "No mlx_lm.server processes running."
    exit 0
fi

echo "Stopping mlx_lm.server (PIDs: $pids)..."
kill $pids
echo "Done."
